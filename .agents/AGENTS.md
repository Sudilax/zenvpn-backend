# ZenVPN Project Rules

## Version Control

**Never run `git add`, `git commit`, or `git push` automatically.**

After completing and verifying changes:
1. Stop and summarize what was changed and verified.
2. Let the user run all git commands themselves.

This rule applies permanently across all sessions on this project.
